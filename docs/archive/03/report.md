# Task archive — 03

## Task

Slug `03`. Skills lock + sheep agents. Branch `main`.

## Story

pipeline skills unlocked · workflow skills locked · sheep-* agents · routing.yaml step map · sync-task + integrate-task git tail · validation fold · pure leaf skills · sheep write boundaries · agent-permissions sheep keys · caveman lite default

## Outcome

Shipped. Archived at `docs/archive/03/`. All 61 subtasks complete.

## Process

**Phase 1** removed skill lock from user-facing pipeline skills; kept lock on workflow-only skills; sheep agents get "Nicki sent you" voice; skills README documents three layers.

**Phase 2** renamed all leaf agents to sheep-*; added routing.yaml; replaced four-step git tail with sync + integrate; folded triage into validation; stripped pipeline refs from leaf skills; updated permissions, smokes, and docs.

**Docs** moved `report.md` and `report-2.md` to `nicki-contruction/`; added `complexity.md` agent load analysis.

**Close** archived to `docs/archive/03/`; construction source torn down.

## Decisions

Parent must never Task-spawn sheep. global-status.json — sheep-start register, sheep-close unregister. status.json — sheep-status only. Legacy commit/push/merge/publish agents and skills removed.

## Suggestions

Finish skills purity pass from report-2 audit on exception workflow skills. Validate close teardown on first real task close.
